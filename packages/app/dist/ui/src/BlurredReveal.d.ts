import React from "react";
type Props = {
    children: React.ReactNode;
    revealed?: boolean;
    duration?: number;
};
export declare function BlurredReveal({ children, revealed, duration, }: Props): import("react/jsx-runtime").JSX.Element;
export {};
